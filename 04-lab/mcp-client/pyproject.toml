[project]
name = "mcp-client"
version = "0.1.0"
description = "Weather Agent using Google ADK with a local Weather MCP server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "google-adk>=2.8,<3",
    "litellm>=1.84",
    "mcp>=1.29,<2",
    "python-dotenv>=1.1.1",
    "httpx>=0.28.1",
]
